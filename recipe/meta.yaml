{% set name = "sisl" %}
{% set version = "0.9.5" %}
{% set sha256 = "ce740c2329415343ed3680164bd3bf9bd652d2284caaf6b12aa86eceb3e624bb" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/zerothi/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1002
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
  host:
    - {{ blas_impl }}
    - libcblas
    - liblapack
    - python
    - setuptools
    - six
    # once we hit 1.16.3 it should be fine again
    # I read that numpy-devel should work, so...
    - numpy <=1.15.3,>=1.16.3
    - scipy >=0.18
    - pyparsing
    - netcdf4
  run:
    - libblas * *{{ blas_impl }}
    - {{ blas_impl }}
    - liblapack
    - python
    - six
    - pyparsing
    - {{ pin_compatible('numpy') }}
    - {{ pin_compatible('scipy') }}
    - netcdf4

test:
  requires:
    - pytest
  imports:
    - sisl
    - sisl.io

about:
  home: http://github.com/zerothi/sisl
  license: LGPL-3.0
  license_family: LGPL
  license_file: LICENSE
  summary: 'Toolbox for electronic structure calculations and large scale transport'
  description: |
    sisl is a tool to manipulate density functional
    theory code input and/or output. It also implements tight-binding
    tools to create and manipulate multi-orbital (non)-orthogonal basis sets.
  doc_url: http://sisl.readthedocs.io/
  dev_url: https://github.com/zerothi/sisl

extra:
  recipe-maintainers:
    - zerothi
